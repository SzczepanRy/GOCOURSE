package main

import "fmt"

func main(){
	//constroll statments  ddeclartations and types
	/*
	if err := do() ;err!=nil{
	errors.New("error")
	}
	*/

        //when running nested loops you cam picka a idemtifier and continue or break that
	/*
	outer:
		for{
			for{
			if smth{

				continue outer || break outer
				}
			}
		}
	*/


	// go switch statments break automaticly
	//--
	//switch on true

	/*
	switch {
	case a<=2:
		do smth
	case b<=3:
		do smth
	default:
		do if no a and no b
	}
	*/
	//when it cones to exports the things beginging with  uppercase are exported 
	
	//if package a users b then b cannot use a 



}
